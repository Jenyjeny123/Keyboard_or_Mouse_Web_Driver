# 调试日志 - 2026-06-27

## 会话概述
本次会话主要解决设备搜索、灯效控制、颜色选择等问题，并进行了 UI 布局优化。

---

## 1. 设备搜索问题 - 鼠标接口被错误识别

### 问题描述
搜索设备时会显示两个接口（键盘 + 鼠标），但只有同时拥有 IN、OUT 端点的接口才能正常工作。

### 尝试的解决方案

#### 方案 1：检查 inputReports 和 outputReports
```javascript
_isKeyboardInterface(device) {
    const hasInput = device.collections?.some(c => c.inputReports?.length > 0);
    const hasOutput = device.collections?.some(c => c.outputReports?.length > 0);
    return hasInput && hasOutput;
}
```
**结果**：失败，WebHID API 的 `collections` 结构复杂，递归检查仍然无法准确判断。

#### 方案 2：递归检查 collections 的所有层级
遍历 `collections` 的 `children` 属性，递归检测端点。
**结果**：失败，结构仍然不符合预期。

#### 方案 3：简化逻辑，依赖 PING 验证
搜索阶段只按 VID/PID 过滤，连接时通过 PING 验证接口类型。
**结果**：部分成功，但用户需要手动尝试两个接口。

#### 最终方案：移除搜索阶段的接口过滤
- 搜索时显示所有匹配 VID/PID 的设备
- 连接时通过 PING 验证，鼠标接口会返回失败
- 用户根据错误提示选择正确的接口

### 结论
WebHID API 无法在搜索阶段准确区分键盘和鼠标接口，需要依赖连接时的 PING 验证。

---

## 2. 灯效控制指令下发错误

### 问题描述
点击"应用"后，下发的指令不一致，有时只发送 `LED_STOP`，有时发送 `LED_START`。

### 根本原因
- Switch 组件初始值是 `false`，但 LightingService 初始值是 `true`，状态不同步
- 当 `enabled=false` 时，所有操作（亮度、速度、模式）都会走 `_stopLighting()` 发送 `LED_STOP`

### 修复方案
1. **统一初始状态**：将 Switch 初始值改为 `true`
2. **应用按钮逻辑**：先启用灯光再下发指令
3. **UI 操作延迟下发**：亮度/速度/模式按钮只更新 UI 状态，不立即下发
4. **状态同步**：颜色选择后同步 `colorIndex` 和 `color` 到 `lighting.state`

### 修改文件
- `index_v3.html`：Switch 初始值、按钮事件、颜色同步逻辑
- `LightingService.js`：`applyState()` 逻辑

---

## 3. 颜色选择问题

### 问题 3.1：颜色选择后立即下发指令
**现象**：点击颜色槽后立即发送指令，而不是等"应用"按钮。

**修复**：
- 颜色槽点击、调色板点击、颜色选择器、HEX 输入都只更新 UI
- 统一通过"应用"按钮下发指令

### 问题 3.2：颜色索引未正确传递
**现象**：选择蓝色（index=5）后，设备日志显示 `mix_color=0`（红色）。

**原因**：颜色选择后没有同步 `colorIndex` 到 `lighting.state`。

**修复**：
```javascript
// 固件预设颜色
if (lighting) lighting.state.colorIndex = index;

// 自定义颜色
if (lighting) {
    lighting.state.colorIndex = 9;
    lighting.state.color = customColor;
}
```

---

## 4. 协议实现问题

### 问题 4.1：SET_PRESET_COLOR 命令不存在
**现象**：调用 `setPresetColor()` 时报错 `Invalid CMD: undefined`。

**原因**：协议文档中没有 `SET_PRESET_COLOR` 命令。

**修复**：根据 PROTOCOL_V2.1_COMPLETE.md 重写颜色逻辑：
- 预设颜色（0-8）：通过 `LED_START` 的 `color` 参数传递
- 自定义颜色（9）：先发送 `SET_CUSTOM_COLOR`，再发送 `LED_START` 带 `color=9`

### 问题 4.2：SET_SINGLE_LED 返回 NOT_SUPPORTED
**现象**：调用 `setMainColor()` 时设备返回 `NOT_SUPPORTED (0x8)`。

**原因**：固件不支持 `SET_SINGLE_LED` 命令。

**修复**：
- 移除 `SET_SINGLE_LED` 的使用
- 改用 `SET_CUSTOM_COLOR` + `LED_START` 组合

### 新增命令
在 `BaseCommands.js` 中添加：
```javascript
SET_CUSTOM_COLOR: C(0x01, 0x0F)  // 0x010F
```

---

## 5. UI 改进

### 5.1 布局调整
- **灯光控制 Tab**：从左右两列改为上下布局
- **LED 预览 + 颜色选择器**：左右并排显示
- **LED 预览按键**：改为填充整个按键背景，移除内部小圆点

### 5.2 颜色选择器优化
- **调色板样式**：添加 30 色调色板网格（6x5）
- **颜色槽**：9 个固件预设 + 1 个自定义颜色
- **交互优化**：点击颜色只更新 UI，不立即下发

### 5.3 按键文字颜色自适应
**问题**：浅色背景上白色文字看不清。

**修复**：
```javascript
function updateLedPreview(color) {
    document.querySelectorAll('.led-key[data-led]').forEach(key => {
        key.style.background = color;
        const rgb = hexToRgb(color);
        if (rgb) {
            const brightness = (rgb.r * 299 + rgb.g * 587 + rgb.b * 114) / 1000;
            key.style.color = brightness > 128 ? '#000000' : '#ffffff';
        }
    });
}
```

### 5.4 下拉菜单显示完整
**问题**：灯效模式下拉菜单选项文字被截断。

**修复**：Select 组件下拉改为 `w-max min-w-full`，选项不再被截断。

---

## 6. 协议流程总结

### 灯效控制流程
```
预设颜色 (0-8):
  → LED_START [mode, speed, brightness, direction, color]

自定义颜色 (9):
  → SET_CUSTOM_COLOR [r, g, b]
  → LED_START [mode, speed, brightness, direction, 9]
```

### 状态同步流程
```
UI 操作（颜色、亮度、速度、模式）
  ↓
更新 lighting.state（不发送指令）
  ↓
点击"应用"按钮
  ↓
applyState() 发送指令到设备
```

---

## 修改文件清单

1. **index_v3.html**
   - Switch 初始值改为 `true`
   - 按钮事件改为只更新 UI 状态
   - 颜色选择同步到 `lighting.state`
   - 添加调色板网格
   - LED 预览文字颜色自适应
   - 布局调整（上下布局、左右并排）

2. **src/services/LightingService.js**
   - 重写 `setPresetColor()` 和 `setMainColor()`
   - 修改 `applyState()` 逻辑
   - 添加 `_applyLighting()` 方法

3. **src/protocol/commands/BaseCommands.js**
   - 添加 `SET_CUSTOM_COLOR` 命令

4. **src/services/DeviceService.js**
   - 简化设备搜索逻辑
   - 移除搜索阶段的接口过滤

5. **src/ui/components/Select.js**
   - 下拉菜单改为 `w-max min-w-full`

---

## 待优化项

1. **设备接口识别**：WebHID API 无法在搜索阶段区分键盘和鼠标接口，需要用户手动选择。
2. **固件兼容性**：部分命令（如 `SET_SINGLE_LED`）固件不支持，需要优雅降级。
3. **状态同步**：UI 状态和 LightingService 状态需要保持一致，避免状态不同步导致的问题。

---

## 调试技巧

1. **查看设备日志**：通过设备端的串口日志确认下发的指令是否正确。
2. **检查状态同步**：在浏览器控制台打印 `lighting.state` 确认状态是否正确。
3. **逐步排查**：从 UI 操作 → 状态更新 → 指令发送 → 设备响应，逐步排查问题。
